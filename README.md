# Safe Unmount Mac

Macで複数の外付けドライブを一括アンマウントする際、カーネルパニック（強制再起動）が起きるのを防ぐためのスクリプトです。

## 特徴
- `sync` コマンドでキャッシュを強制フラッシュ
- 1ドライブごとに2秒の待機時間を挟み、OSの負荷を分散
- HFS+形式のドライブとオーディオドライバの競合によるクラッシュを回避

## 使い方
1. スクリプトに実行権限を与える: `chmod +x safe_eject.sh`
2. 実行する: `./safe_eject.sh`